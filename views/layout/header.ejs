<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- En el <head> de header.ejs, después del CSS -->
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
    <title><%= titulo ? titulo + ' | ' : '' %>H. Ayuntamiento de Las Choapas</title>
    
    <!-- ⚠️ EL CSS SOLO VA AQUÍ, UNA SOLA VEZ -->
    <link rel="stylesheet" href="/css/main.css">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Animaciones -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
</head>
<body>
    <header class="header">
        <nav class="navbar container">
            <div class="logo">
    <a href="/">
        <img src="/imagenes/LOGO.png" alt="Las Choapas" class="logo-img">
       
    </a>
</div>
            <ul class="nav-menu">
                <li class="<%= currentPage === 'inicio' ? 'active' : '' %>">
                    <a href="/">Inicio</a>
                </li>
                <li class="<%= currentPage === 'gobierno' ? 'active' : '' %>">
                    <a href="/gobierno">Gobierno</a>
                </li>
                <li class="<%= currentPage === 'tramites' ? 'active' : '' %>">
                    <a href="/tramites">Trámites</a>
                </li>
                <li class="<%= currentPage === 'contacto' ? 'active' : '' %>">
                    <a href="/contacto">Contacto</a>
                </li>
            </ul>
        </nav>
    </header>
    <main>